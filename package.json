{
  "name": "phoenix-terminal",
  "version": "3.0.0",
  "description": "Cyberpunk trading terminal for Phoenix perpetuals — web-only swarm bot",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd web && npm run dev",
    "build": "cd web && npm run build",
    "start": "cd web && npm start"
  },
  "engines": {
    "node": ">=20"
  }
}
